import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Chats = () => {
  return (
    <SafeAreaView>
      <Text>Hello this is page Chats</Text>
    </SafeAreaView>
  );
};

export default Chats;
