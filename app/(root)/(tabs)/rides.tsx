import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Ride = () => {
  return (
    <SafeAreaView>
      <Text>Hello this is page rides</Text>
    </SafeAreaView>
  );
};

export default Ride;
