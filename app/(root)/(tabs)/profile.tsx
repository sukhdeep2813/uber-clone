import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text>Hello this is page profile</Text>
    </SafeAreaView>
  );
};

export default Profile;
